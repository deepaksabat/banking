<%= simple_form_for @deposite do |f| %>
  <% if @deposite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deposite.errors.count, "error") %> prohibited this deposite from being saved:</h2>

      <ul>
      <% @deposite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-inputs">
    <%= f.input_field :date, class: "form-controle datepicker", as: :string %>
    <%= f.input :ammount %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
